endpoints {
  authorization {
    none
    // or add OAuth configuration
  }

  action-endpoints {
    action-endpoint (LoLChampionRecommend) {
      local-endpoint (lolChampionRecommend.js)
    }
  }
}
